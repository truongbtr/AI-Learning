{
  "name": "@mtct/core",
  "version": "0.1.0",
  "private": true,
  "description": "Pure domain logic (no Next, no Prisma): auth policy, calendar, storage adapter",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./storage": "./src/storage/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@mtct/config": "workspace:*",
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0",
    "vitest": "^5.0.0"
  },
  "files": [
    "src",
    "package.json"
  ]
}
