{
  "name": "@mtct/worker",
  "version": "0.1.0",
  "private": true,
  "description": "pg-boss worker: scheduled jobs (ping in phase 0; planner, decay, intake preprocessing later)",
  "type": "commonjs",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@mtct/core": "workspace:*",
    "@mtct/db": "workspace:*",
    "dotenv": "^17.0.0",
    "pg-boss": "^12.0.0",
    "pino": "^10.0.0",
    "tsx": "^4.20.0"
  },
  "devDependencies": {
    "@mtct/config": "workspace:*",
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0",
    "vitest": "^5.0.0"
  }
}
